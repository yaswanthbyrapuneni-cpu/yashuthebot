[pytest]
testpaths = tests
# conftest.py lives in tests/ and is imported by the test modules directly.
pythonpath = . tests
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
